apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-route
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`nextcloud.cscms.me`)
      kind: Rule
      services:
        - name: nextcloud
          kind: Service
          port: 8080
