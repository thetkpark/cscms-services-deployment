apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cscms-intern-pvc
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
